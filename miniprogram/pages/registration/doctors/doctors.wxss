.doctors-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 24rpx;
}

.doctors-header__dept {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--color-text);
}
